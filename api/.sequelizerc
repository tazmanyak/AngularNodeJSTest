const path = require('path');

module.exports = {
    'migration-path': path.resolve('db','migrations'),
    'models-path': path.resolve('db','models'),
    'config': path.resolve('config', 'database.json')
}
